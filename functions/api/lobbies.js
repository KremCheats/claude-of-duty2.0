export async function onRequestGet({ env }) { const result = await env.MERK_DB?.prepare('SELECT lobby_id, host_name, mode, map_id, max_players, current_players, is_private FROM lobbies WHERE status = "open" ORDER BY created_at DESC LIMIT 50').all(); return Response.json(result?.results || []); }
export async function onRequestPost({ request, env }) { const b = await request.json(); const id = crypto.randomUUID(); await env.MERK_DB?.prepare('INSERT INTO lobbies (lobby_id, host_name, mode, map_id, max_players, current_players, is_private, status) VALUES (?, ?, ?, ?, ?, 1, ?, "open")').bind(id, String(b.host_name || 'OPERATIVE'), String(b.mode || 'tdm'), String(b.map_id || 'hijacked'), Math.min(20, Number(b.max_players || 10)), b.is_private ? 1 : 0).run(); return Response.json({ ok: true, lobby_id: id }); }
